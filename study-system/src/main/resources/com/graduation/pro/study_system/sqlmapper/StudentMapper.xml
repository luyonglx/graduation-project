<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.pro.study_system.mapper.StudentDao">
	<insert id="insertStu" parameterType="Student" keyProperty="stuId">
		insert into student(
		stu_name)
		values
		(
		#{stuName})
	</insert>
</mapper>